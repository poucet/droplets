[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
authors = ["Simply Chris"]
publish = false

[dependencies]
# For the bundling tasks, we'll use the nih_plug_xtask crate
nih_plug_xtask = { git = "https://github.com/robbert-vdh/nih-plug.git" }
