# Droplets Project Instructions

## Version Control

This project uses `jj` (Jujutsu) instead of git.

- Use `jj commit -m "message"` to create commits (not `jj describe`)
- Use `jj status` to check working copy state
- Use `jj diff` to see changes
- Use `jj log` to view history
