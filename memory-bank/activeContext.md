# Active Context: Granular VST

## Current Focus
- Initial project setup using `cargo`.
- Integrating `nih-plug` and `nih-plug-iced` dependencies.
- Creating the basic plugin structure based on `nih-plug` examples.

## Recent Changes
- Created initial Memory Bank files (`projectbrief.md`, `productContext.md`).

## Next Steps
- Initialize the Rust project directory using `cargo new`.
- Add `nih-plug` and `nih-plug-iced` to `Cargo.toml`.
- Create the main library file (`src/lib.rs`) with the basic plugin struct and `nih_export` macros.
